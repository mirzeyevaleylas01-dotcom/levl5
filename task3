#ikiededböyüyütap
#include <iostream>
using namespace std;

void big(int a, int b){
    if(a > b) cout << a << endl;
    else cout << b << endl;
}

int main(){
    int x, y;
    cin >> x >> y;
    big(x, y);
}

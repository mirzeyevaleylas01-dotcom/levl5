#ucededboyuyutap
#include <iostream>
using namespace std;

void big3(int a, int b, int c){
    int max = a;
    if(b > max) max = b;
    if(c > max) max = c;
    cout << max << endl;
}

int main(){
    int x, y, z;
    cin >> x >> y >> z;
    big3(x, y, z);
}
